/**
 * @file File contains Aggregate Utility Methods.
 * @author Shubham Singh
 */

const envUtil = require('./env');
const mailUtil = require('./mail');
const validationUtil = require('./validation');

module.exports = {
  envUtil,
  mailUtil,
  validationUtil
};
