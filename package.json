{
  "name": "API-LoopBack",
  "version": "1.0.0",
  "main": "server/server.js",
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "lint": "eslint .",
    "dev:server": "NODE_ENV=development nodemon server/server.js -watch common --watch server",
    "test:server": "NODE_ENV=test nodemon server/server.js -watch common --watch server",
    "start": "node .",
    "test:watch": "NODE_ENV=test mocha --watch",
    "test": "npm-run-all -p -r test:server test:watch"
  },
  "dependencies": {
    "compression": "^1.0.3",
    "cors": "^2.5.2",
    "helmet": "^3.10.0",
    "loopback": "^3.22.0",
    "loopback-boot": "^2.6.5",
    "loopback-component-explorer": "^6.2.0",
    "nodemon": "^1.18.4",
    "serve-favicon": "^2.0.1",
    "strong-error-handler": "^3.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^3.17.1",
    "eslint-config-loopback": "^8.0.0",
    "mocha": "^5.2.0",
    "supertest": "^3.3.0"
  },
  "repository": {
    "type": "",
    "url": ""
  },
  "license": "UNLICENSED",
  "description": "API-LoopBack"
}
